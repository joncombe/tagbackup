# Built binary
/tagbackup
tagbackup
*.exe

# GoReleaser output (repo root only; internal/server/dist is the committed,
# embedded web UI build and must NOT be ignored)
/dist/

# Frontend dev dependencies
/web/node_modules/

.DS_Store
